# cert-expiring terminator: valid now, notAfter < 14 days away.
server {
    listen 443 ssl;
    server_name expiring;
    ssl_certificate     /certs/expiring.crt;
    ssl_certificate_key /certs/expiring.key;
    location / { return 200 "range expiring-soon-cert host\n"; }
}
