# cert-self-signed terminator: serves a cert not chained to the range CA.
server {
    listen 443 ssl;
    server_name self-signed;
    ssl_certificate     /certs/self-signed.crt;
    ssl_certificate_key /certs/self-signed.key;
    location / { return 200 "range self-signed-cert host\n"; }
}
