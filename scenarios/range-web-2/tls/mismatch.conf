# cert-mismatch terminator: cert CN/SAN belongs to a different hostname.
server {
    listen 443 ssl;
    server_name mismatch;
    ssl_certificate     /certs/mismatch.crt;
    ssl_certificate_key /certs/mismatch.key;
    location / { return 200 "range name-mismatch-cert host\n"; }
}
