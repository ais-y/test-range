# cert-expired terminator: serves the expired cert (notAfter in the past).
server {
    listen 443 ssl;
    server_name expired;
    ssl_certificate     /certs/expired.crt;
    ssl_certificate_key /certs/expired.key;
    location / { return 200 "range expired-cert host\n"; }
}
